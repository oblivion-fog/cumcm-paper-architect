---
name: cumcm-paper-architect
description: >
  将零散的赛题描述、代码逻辑和运行结果转化为规范的数学建模竞赛 LaTeX 论文。
  当用户提供赛题PDF/截图、Python/MATLAB 求解代码、建模笔记、Excel数据或运行结果时，
  使用此技能自动生成符合 CUMCM(国赛)/MCM-ICM(美赛)/华东杯/亚太杯/深圳杯格式标准的 .tex 论文文件。
  核心能力包括：从代码逆向推导数学模型、提取目标函数和约束条件、生成符号说明和模型假设、
  构建完整的论文结构（摘要→问题重述→问题分析→模型建立与求解→模型检验→参考文献→附录）。
  触发场景：用户提到"写论文"、"数模论文"、"建模论文"、"latex论文"、"国赛论文"、
  "华东杯"、"美赛"、"MCM"、"ICM"、"亚太杯"、"帮我写论文"、"根据代码写论文"、
  "根据赛题写论文"、"把代码转成论文"、"生成latex论文"时触发。
  即使用户只是简单说"帮我写数模论文"、"整理成论文"、"搞一篇论文"，
  或者只提供了赛题+代码+结果而没有明确说要写论文，也请使用此技能。
  用户提到遗传算法、蒙特卡洛、模拟退火、粒子群、神经网络等算法并说要写成论文时也应触发。
---

# 数模论文智能架构师

## 工作流概览

```
输入（题目+代码+笔记+结果） → 材料解析 → 完备性检查 → 数学形式化 → LaTeX 生成
```

整个流程分四步，按顺序执行。如果材料不全，在第二步就主动追问，不要跳过。

---

## 第一步：多源材料解析

用户可能提供以下任意组合，逐一读取并理解：

| 材料类型 | 提取内容 |
|---------|---------|
| 题目描述 / 截图 | 赛题编号、子问题、已知数据、约束条件 |
| Python 代码 | 目标函数、约束条件、算法名称及参数、输入输出变量 |
| MATLAB 代码 | 同上，注意 MATLAB 的矩阵运算习惯写法 |
| 建模笔记（零散文字） | 建模思路、假设、未完成的分析 |
| 运行结果 / 图表数据 | 定量数值、图表含义、对比结论 |

### 代码逆向建模要点

阅读代码时，主动识别以下元素并记录：

1. **目标函数** — 代码中 `minimize`、`maximize`、`loss`、`obj` 等关键字段
2. **约束条件** — 不等式约束、等式约束、边界条件
3. **算法流程** — 遗传算法（种群大小、交叉率、变异率、迭代次数）、蒙特卡洛（模拟次数、随机种子）、数值优化（步长、收敛判据）等
4. **输入输出变量** — 哪些是已知量，哪些是待求量，哪些是中间变量

如果代码中出现了你不认识的算法或函数，先搜索理解，再写入论文。

---

## 第二步：材料完备性检查（主动追问）

在开始生成论文前，快速检查以下关键要素是否齐全。**缺失时主动追问用户**，不要自行编造数据：

- [ ] 每个子问题是否都有对应的模型或求解结果？
- [ ] 是否有灵敏度分析的结果或数据？（如缺少，提示用户补充）
- [ ] 代码中涉及随机过程/迭代的，是否有收敛性分析？（如缺少，提示用户）
- [ ] 是否有模型检验（误差分析、残差分析、交叉验证等）？
- [ ] 是否有可视化图表的对应数据或截图描述？

追问示例：
> 检测到代码中使用了遗传算法（种群=50，迭代=200），但未看到收敛性分析或迭代曲线数据。是否需要补充？
> 目前缺少针对参数 $v_0$ 的灵敏度分析数据，请提供波动测试结果。

如果用户确认"先写，后面再补"，则继续生成，并在对应位置标注 `[待补充]`。

---

## 第三步：数学形式化

### 3.1 问题分析

从建模者的视角拆解每个子问题的内在关联：
- 各子问题之间是递进关系还是并列关系？
- 问题一的结果是否是问题二的输入？
- 模型选择背后的动机是什么？

建议在文字说明的同时用流程图描述总体思维过程，这会使思维逻辑一目了然。

### 3.2 模型假设

**模型假设必须用文字段落描述，不能用表格。** 每条假设独立成段，带编号。

假设类型包括：
1. 对题目已知条件或参数做保真性假设（如数据准确、无异常值）
2. 仅考虑主要条件，对其他情况不考虑或强制规定
3. 对主要条件做平稳性规定（如系统温度恒定）
4. 为使研究更简便、从常识角度做出的假设
5. 对模型中相关参数做出规定

示例写法：
```latex
\textbf{假设 1：}题目给出的车辆成本数据和排货信息准确可靠，不存在录入异常值。

\textbf{假设 2：}公路运输过程中除行驶里程和装卸时间外，不考虑交通拥堵、临时交通管制等不确定因素对运输时间的影响。

\textbf{假设 3：}同一交接单号下的药品可以在同一批次完成装载和发车，不考虑分批发车的情况。
```

不要写"假设数据真实可靠"这种空洞的话，每条假设必须与后续模型直接相关。

### 3.3 符号说明

**符号说明紧跟在"模型假设"章节之后、"模型建立与求解"章节之前，作为正文的第4章。绝对不能放到附录里。**

符号说明用三线表形式，格式如下：

```latex
\begin{table}[h]
\centering
\caption{本文的主要符号说明}
\label{tab:symbols}
\begin{tabular}{cll}
\toprule
符号 & 含义 & 单位 \\
\midrule
$\alpha$ & 非直线系数 & --- \\
$v$ & 平均行驶速度 & km/h \\
$L$ & 往返公路里程 & km \\
$p_i$ & 第 $i$ 个订单的托盘数 & 托 \\
\bottomrule
\end{tabular}
\end{table}
```

注意：
- 只写主要的全局变量，临时变量不需要写
- 建议用希腊字母，不要用中文字符
- 即使在符号说明里解释过，也要在下文该符号首次出现时再次说明

---

## 第四步：LaTeX 论文生成

### 4.1 论文结构（严格遵循 CUMCM 国赛模板）

```
封面页（参赛编号、题目）
─────────────────────────
摘要 + 关键词
1. 问题重述
2. 问题分析
   2.1 问题一的分析
   2.2 问题二的分析
   2.3 问题三的分析
3. 模型假设（文字段落描述，编号形式）
4. 符号说明（三线表）
5. 问题一模型的建立与求解
   5.1 数据预处理
   5.2 XXX模型的建立
   5.3 XXX模型的求解
   5.4 XXX结果的分析
6. 问题二模型的建立与求解
   6.1 数据预处理
   6.2 XXX模型的建立
   6.3 XXX模型的求解
   6.4 XXX结果的分析
7. 问题三模型的建立与求解（如有）
   （同上结构）
8. 模型检验与评价
   8.1 模型检验（稳定性/敏感性/误差分析）
   8.2 模型的优点
   8.3 模型的缺点
   8.4 模型的改进
参考文献
附录
```

### 4.2 摘要规范

**摘要是最重要的部分，占一整页，认真写。** 包含以下内容：

**开头段（3-5行）**：背景 + 主要解决的问题 + 整体方法。
```latex
本文针对医药物流调度中的高成本与低效率问题，建立了从运输费用计算到拼单优化的
完整数学模型体系。通过建立带容量约束和时间窗约束的车辆路径问题（CVRPTW）模型，
设计了四层启发式求解架构，对南通仓库出发的全国配送方案进行了系统优化。
```

**正文部分（每个问题一段）**：针对问题X，通过XXX分析，建立XXX模型，基于XXX方法/软件，求解得到XXX结果。
```latex
针对问题一，在现有独立派车方案下，建立了基于固定成本日摊销与变动成本按里程
计算的运输费用模型。通过容量修正公式解决了物理不可行记录，改进地址解析算法
消除了远途距离误差。计算得运输总费用为 \textbf{¥315,814.43}，其中超长途
（$>$1000 km）运输占总成本的 91.5\%，车辆容量利用率不足 30\%。
```

**结尾段**：对整个建模过程的总结，包括优缺点评价、创新性评价、模型推广。

### 4.3 问题重述规范

- 不是简单复制题目原文，要用自己的话改写
- 简要说明问题情景（来龙去脉）
- 列出必要数据，提出要明确的问题
- 给出研究对象的关键信息
- **关键：改写！** 用自己的语言重新组织问题描述
- 篇幅在半页到一页左右

### 4.4 问题分析规范

- 承上启下的桥梁，反映建模者对问题的认识程度
- 包含：题目信息和条件、整体分析、确定建模方法
- 每个问题单独分析一小节
- 分析过程简明扼要，不放结论
- **用流程图或图表辅助说明思维过程**
- 可以放一个总体分析流程图

### 4.5 模型建立与求解规范（核心！）

**这是论文最重要的一环。** 每个问题的章节统一按以下子结构：

#### 5.1 数据预处理

描述数据来源、预处理步骤、清洗逻辑。用文字说明，不是只列公式。

```latex
首先对附件2的排货数据进行预处理。原始数据共68行明细记录，经分析发现同一
交接单号（handover\_no）代表一次独立派车，可能存在多明细行。因此按交接单号
进行聚合去重，托盘数求和，其他字段取首值，得到24次独立派车任务。

此外，对车型和目的地地址进行了规范化处理。地址解析采用字典前缀匹配算法，
优先匹配``XX市/省/自治州''正则表达式，避免``乌鲁木齐市\ldots 长春南路119''
被误判为吉林长春的歧义。
```

#### 5.2 模型的建立

**不只是列公式，必须有充分的文字描述：**

1. **建模动机**：为什么选这个模型？问题本质是什么？
2. **模型推导**：从原理出发逐步推导，解释每一步的物理/数学含义
3. **公式呈现**：公式要编号，解释每个符号
4. **约束说明**：每个约束条件的实际含义是什么
5. **与代码对应**：文中模型与代码逻辑必须一致

示例写法：
```latex
\subsubsection{成本模型}

对于每次独立派车任务，运输费用由固定成本和变动成本两部分构成。固定成本
包括保险、年审、维保、人工、轮胎损耗等按天摊销的费用；变动成本包括油耗和
路桥费，按行驶里程计算。

\textbf{（1）单趟费用公式。}设第 $i$ 次派车选用车型为 $\text{type}_i$，
用车数为 $n_i$，则单趟费用为：
\begin{equation}
\text{Cost}_i = n_i \cdot \left( F_{\text{day}}^{(\text{type}_i)} \cdot T_i
+ V_{\text{km}}^{(\text{type}_i)} \cdot L_i \right)
\label{eq:single_cost}
\end{equation}
其中 $F_{\text{day}}^{(\text{type}_i)}$ 为日固定成本（元/天），$T_i$ 为单趟
耗时（天），$V_{\text{km}}^{(\text{type}_i)}$ 为单公里变动成本（元/km），
$L_i$ 为往返公路里程（km）。

\textbf{（2）往返里程估算。}由于题目仅给出目的地的地址信息，采用 Haversine
公式计算南通仓库与目的地的球面直线距离 $d_i$，再乘以非直线系数 $\alpha$
得到实际公路里程。往返里程为：
\begin{equation}
L_i = 2\alpha d_i
\label{eq:round_trip}
\end{equation}
正常天气取 $\alpha=1.3$，恶劣天气取 $\alpha=1.5$（参考附件3天气数据）。

\textbf{（3）容量修正。}原始数据中存在 vehicle\_count=1 但 pallets=42.49
的物理不可行记录（1辆7.6m车额定容量仅18托）。对此进行容量约束修正：
\begin{equation}
n_i^{\ast} = \max\left(n_i^{\text{录入}},\ \left\lceil \frac{p_i}
{C_{\text{cap}}^{(\text{type}_i)}} \right\rceil\right)
\label{eq:capacity_fix}
\end{equation}
```

**关键原则：**
- 每个公式之前有引入文字，说明为什么要引入这个公式
- 每个公式之后有解释文字，说明公式中各部分的含义
- 公式编号用 `\label{eq:xxx}`，后文引用用 `\eqref{eq:xxx}`

#### 5.3 模型的求解

描述求解过程、算法步骤、参数设置。复杂算法用伪代码或流程图。

```latex
本文采用四层启发式架构对 CVRPTW 模型进行求解，整体流程如图 \ref{fig:q2_flow} 所示。

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{figures/q2_flow_chart.png}
\caption{问题二四层求解架构图}
\label{fig:q2_flow}
\end{figure}

\textbf{第一层：日期分组。}按预计到货日期将订单分组\ldots

\textbf{第二层：Lookahead 拼单。}方案A仅当日内合并；方案B同区域跨日合并\ldots
```

#### 5.4 结果的分析

**必须有表格 + 文字解读 + 图表引用。** 不是只放一个数字。

```latex
\begin{table}[h]
\centering
\caption{问题一主要计算结果}
\label{tab:q1_results}
\begin{tabular}{lr}
\toprule
指标 & 数值 \\
\midrule
独立派车趟次 & 24 \\
运输总费用 & \textbf{¥315,814.43} \\
平均每趟 & ¥13,159 \\
7.6m车型（17趟） & ¥239,242（75.7\%） \\
9.6m车型（7趟） & ¥76,573（24.3\%） \\
超长途 $>$1000 km（19趟） & ¥288,874（91.5\%） \\
\bottomrule
\end{tabular}
\end{table}

由表 \ref{tab:q1_results} 可知，运输总费用为 ¥315,814.43。从结构来看，
超长途（$>$1000 km）运输共19趟，占总费用的 91.5\%，说明远距离独立派车
是成本的主要来源。

图 \ref{fig:q1_flow} 展示了从南通仓库辐射全国的派车流向，箭头宽度代表托盘
数量。可以看出远途订单分布密集，存在显著的拼单优化空间。

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/q1_flow_map.png}
\caption{南通仓库全国派车流向图（箭头宽度=托盘数）}
\label{fig:q1_flow}
\end{figure}

进一步分析发现，修正前由于地址解析错误（乌鲁木齐误判为长春，偏差2300 km）和
容量约束缺失，总费用被低估为 ¥228,551，修正后偏差达 38\%。这说明数据预处理
环节对最终结果的准确性至关重要。
```

### 4.6 图表插入规范

**论文中必须在相应部分插入合适的图表**，不能只放公式不配图。

| 章节 | 建议图表类型 |
|------|-------------|
| 问题分析 | 总体分析流程图 |
| 问题一 | 数据流向图、成本结构图、散点/柱状/瀑布图 |
| 问题二 | 求解架构图、VRPTW示意图、路线图、对比柱状图、灵敏度曲线 |
| 问题三 | 雷达图、优化前后对比图、容量利用率分布图 |
| 模型检验 | 残差图、收敛曲线 |
| 附录 | 甘特图、桑基图、详细数据表 |

图表格式：
```latex
\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/图文件名.png}
\caption{图表标题（简明扼要，说明图的核心内容）}
\label{fig:图标签}
\end{figure}
```

如果用户没有提供图表文件，在对应位置插入 `\textbf{[图X待插入：描述]}` 占位符，告知用户需要补充什么图。

### 4.7 排版与字体规范

**标题居中：** 所有标题（论文题目、一级标题、二级标题等）均需居中对齐。包括无编号标题（如附录标题）。

**字体字号：**
- 论文题目：三号黑体（`\fontsize{16pt}{18pt}\selectfont\heiti`）
- 一级标题：四号黑体（`\fontsize{14pt}{16pt}\selectfont\heiti`）
- 二级及以下标题：小四号黑体（`\fontsize{12pt}{14pt}\selectfont\heiti`）
- 正文：小四号宋体（`\fontsize{12pt}{12pt}\selectfont`），单倍行距（`\renewcommand{\baselinestretch}{1.0}`）

**公式、图、表编号：**
- 公式统一编号为 `(章节号.序号)` 格式，如 `(5.1)`、`(5.2)`
- 图表统一编号，按全文顺序连续编号
- 图表标题居中显示

**摘要规范：** 摘要具有独立性和自明性，内容完整，篇幅不超过一页。关键词顶格排版（左对齐，`\noindent\textbf{关键词：}...`），不使用居中环境。

**页码与目录：**
- 论文第一页为封面（无页码）
- 论文第二页为目录（`\tableofcontents`）
- 第三页起为正文
- 页码从正文第一页开始编号，使用阿拉伯数字从 `1` 连续计数
- 页码位于每页页脚中部（`\cfoot`）

### 4.8 LaTeX 模板

**论文开头的 LaTeX 模板必须包含以下完整结构：**

```latex
\documentclass[12pt,a4paper]{article}
\usepackage[UTF8]{ctex}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{hyperref}
\usepackage{cite}
\usepackage{array}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{float}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage[justification=centering]{caption}
\usepackage{setspace}

\geometry{left=2.5cm,right=2.5cm,top=3cm,bottom=2.5cm}

% 页眉页脚设置
\pagestyle{fancy}
\fancyhf{}
\cfoot{\thepage}
\renewcommand{\headrulewidth}{0pt}

% 标题居中（含无编号标题）
\titleformat{\section}[block]
  {\centering\fontsize{14pt}{16pt}\selectfont\heiti}
  {\thesection}{1em}{}
\titleformat{\subsection}[block]
  {\centering\fontsize{12pt}{14pt}\selectfont\heiti}
  {\thesubsection}{1em}{}
\titleformat{\subsubsection}[block]
  {\centering\fontsize{12pt}{14pt}\selectfont\heiti}
  {\thesubsubsection}{1em}{}
\titleformat{name=\section,numberless}[block]
  {\centering\fontsize{14pt}{16pt}\selectfont\heiti}
  {}{0em}{}

% 公式编号 (章节号.序号)
\renewcommand{\theequation}{\arabic{section}.\arabic{equation}}
\numberwithin{equation}{section}
\numberwithin{figure}{section}
\numberwithin{table}{section}

% 正文字体字号
\renewcommand{\baselinestretch}{1.0}

% 图表标题居中
\captionsetup[figure]{justification=centering}
\captionsetup[table]{justification=centering}
\captionsetup[algorithm]{justification=centering}

% 论文题目：三号黑体
\title{\fontsize{16pt}{18pt}\selectfont\heiti 论文题目}
\author{}
\date{}

\begin{document}

% 封面页（无页码）
\maketitle
\thispagestyle{empty}
\newpage

% 目录页（\tableofcontents 自动生成居中标题，不要手动添加）
\tableofcontents
\thispagestyle{empty}
\newpage

% 正文开始，页码从1计数
\setcounter{page}{1}
\fontsize{12pt}{12pt}\selectfont

% 附录纳入目录层级（使用 \section* + 手动 \addcontentsline）
% 标题正文中不显示"附录A"前缀，仅目录中显示
\appendix
\renewcommand{\thesection}{附录\Alph{section}}
\section*{附录标题}
\addcontentsline{toc}{section}{\thesection\quad 附录标题}
```

### 4.9 术语与风格约束

- 使用标准数学建模术语，杜绝口语化表达
- 公式必须有编号，后文引用用 `\eqref{}`
- 图表必须有标题和编号，正文中要引用（"如图X所示"、"表X表明"）
- 避免"我们做了"，使用"本文建立了"、"研究表明"
- 定量结果保留 3-4 位有效数字
- **模型假设用文字段落编号，不用表格**
- **符号说明紧跟在模型假设之后**
- **每个问题章节必须包含：数据预处理描述 + 模型建立的文字说明 + 求解过程的文字描述 + 结果分析的文字描述 + 图表**

### 4.10 逻辑自洽性检查

生成完成后，自检：

- [ ] 标题是否全部居中对齐？
- [ ] 论文题目是否为三号黑体？一级标题是否为四号黑体？正文是否为小四号宋体、单倍行距？
- [ ] 摘要是否不超过一页且具有自明性？关键词是否顶格左对齐（非居中）？
- [ ] 目录标题是否仅出现一次（\tableofcontents 自动生成，无重复手动标题）？
- [ ] 附录是否使用 \section* + \addcontentsline 纳入目录？目录显示"附录A"前缀，正文标题无前缀？
- [ ] 第二页是否为目录？正文是否从第三页开始？
- [ ] 页码是否从正文开始，使用阿拉伯数字从"1"连续编号，位于页脚中部？
- [ ] 公式编号是否为 (章节号.序号) 格式？图、表标题是否居中并统一编号？
- [ ] 符号表中定义的每个符号是否在文中正确使用？
- [ ] 论文中提到的模型是否与代码中的算法一致？
- [ ] 所有公式是否有编号？所有图表是否有引用？
- [ ] 摘要中的数值结果是否与代码运行结果一致？
- [ ] 各子问题之间的逻辑衔接是否连贯？
- [ ] 模型假设是否用文字段落（非表格）描述？
- [ ] 符号说明是否在模型建立之前（非论文结尾）？
- [ ] 每个问题章节是否有充分的文字描述（非只有公式）？
- [ ] 是否在对应部分插入了图表或占位符？

---

## 输出

最终输出一份完整的 `.tex` 文件。文件应可直接用 `xelatex` 编译生成 PDF。

如果用户提供了具体的参赛编号或题目编号（如 "CUMCM 2024 B题"），自动填入封面信息。
